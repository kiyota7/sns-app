<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snsapp.mapper.FollowMapper">

    <select id="existsByFollowerAndFollowed" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM follows WHERE follower_id = #{followerId} AND followed_id = #{followedId}
        )
    </select>

    <insert id="insert">
        INSERT INTO follows (follower_id, followed_id)
        VALUES (#{followerId}, #{followedId})
    </insert>

    <delete id="delete">
        DELETE FROM follows WHERE follower_id = #{followerId} AND followed_id = #{followedId}
    </delete>

</mapper>

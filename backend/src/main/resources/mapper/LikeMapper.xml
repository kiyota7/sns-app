<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snsapp.mapper.LikeMapper">

    <select id="existsByPostIdAndUserId" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM likes WHERE post_id = #{postId} AND user_id = #{userId}
        )
    </select>

    <insert id="insert">
        INSERT INTO likes (post_id, user_id)
        VALUES (#{postId}, #{userId})
    </insert>

    <delete id="delete">
        DELETE FROM likes WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <delete id="deleteByPostId">
        DELETE FROM likes WHERE post_id = #{postId}
    </delete>

    <select id="countByPostId" resultType="int">
        SELECT COUNT(*) FROM likes WHERE post_id = #{postId}
    </select>

</mapper>
